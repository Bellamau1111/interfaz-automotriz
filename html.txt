<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Simulador Automotriz</title>
  <style>
    body {
      margin: 0;
      font-family: sans-serif;
      background-color: #1e1e1e;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 10px;
    }
    .button-row {
      display: flex;
      gap: 10px;
      margin-bottom: 10px;
    }
    .cmd-btn {
      width: 60px;
      height: 60px;
      border: none;
      border-radius: 8px;
      font-size: 12px;
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    .red { background-color: #c0392b; }
    .green { background-color: #27ae60; }
    .connect-btn {
      background-color: #2ecc71;
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      border: none;
      font-size: 16px;
      margin-bottom: 15px;
    }
    .status-panel, .diagnostic-panel, .event-log {
      width: 100%;
      max-width: 400px;
      margin-bottom: 15px;
      padding: 10px;
      background-color: #2c2c2c;
      border-radius: 8px;
    }
    .status-item {
      display: flex;
      align-items: center;
      margin-bottom: 5px;
    }
    .status-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      margin-right: 8px;
    }
    .red-dot { background-color: red; }
    .green-dot { background-color: limegreen; }
    .diagnostic-item {
      margin-bottom: 5px;
    }
    .event-log p {
      margin: 4px 0;
      font-size: 12px;
    }
    .event-red { color: #e74c3c; }
    .event-green { color: #2ecc71; }
    .event-yellow { color: #f1c40f; }
    .event-gray { color: #bdc3c7; }
  </style>
</head>
<body>

  <div class="button-row">
    <button class="cmd-btn red" onclick="sendCommand('LOCK')">🔒<br>Cmd 1</button>
    <button class="cmd-btn green" onclick="sendCommand('UNLOCK')">🔓<br>Cmd 2</button>
    <button class="cmd-btn red" onclick="sendCommand('FUEL_OFF')">⛽<br>Cmd 3</button>
    <button class="cmd-btn green" onclick="sendCommand('FUEL_ON')">⛽<br>Cmd 4</button>
  </div>

  <button class="connect-btn" onclick="simulateConnect()">Conectar con Auto</button>

  <div class="status-panel">
    <div class="status-item"><div class="status-dot red-dot"></div>Emergencia ON</div>
    <div class="status-item"><div class="status-dot green-dot"></div>Modo seguro</div>
  </div>

  <div class="diagnostic-panel">
    <div class="diagnostic-item" id="temp">🌡️ Temperatura: 52.6 °C</div>
    <div class="diagnostic-item" id="volt">⚡ Voltaje alternador: 11.9 V</div>
    <div class="diagnostic-item" id="oil">💧 Presión aceite: OK</div>
    <div class="diagnostic-item" id="ign">🔑 Ignición: ON</div>
  </div>

  <div class="event-log" id="eventLog">
    <p class="event-red">[0:12] Bloqueo puertas activado</p>
    <p class="event-green">[0:09] Voltaje normal 12 a V</p>
    <p class="event-yellow">[0:09] Arranque aceite pendiente</p>
    <p class="event-gray">[0:05] Listo: 1=12 ON, 2=14 ON</p>
    <p class="event-gray">[0:02] Calibración: Volt 12.3 V</p>
  </div>

<script>
function simulateConnect() {
  logEvent("Conexión simulada con vehículo", "event-green");
  setInterval(() => {
    const temp = (50 + Math.random() * 10).toFixed(1);
    const volt = (11 + Math.random() * 2).toFixed(1);
    document.getElementById("temp").innerText = `🌡️ Temperatura: ${temp} °C`;
    document.getElementById("volt").innerText = `⚡ Voltaje alternador: ${volt} V`;
  }, 3000);
}

function sendCommand(cmd) {
  logEvent(`Comando enviado: ${cmd}`, "event-yellow");
  if (cmd === "LOCK") logEvent("Puertas bloqueadas", "event-red");
  if (cmd === "UNLOCK") logEvent("Puertas desbloqueadas", "event-green");
  if (cmd === "FUEL_OFF") logEvent("Bomba de combustible apagada", "event-red");
  if (cmd === "FUEL_ON") logEvent("Bomba de combustible encendida", "event-green");
}

function logEvent(text, style) {
  const log = document.getElementById("eventLog");
  const time = new Date().toLocaleTimeString().slice(0,5);
  const p = document.createElement("p");
  p.className = style;
  p.innerText = `[${time}] ${text}`;
  log.prepend(p);
  if (log.children.length > 10) log.removeChild(log.lastChild);
}
</script>

</body>
</html>